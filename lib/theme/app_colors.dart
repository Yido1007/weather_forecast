import 'package:flutter/material.dart';

class AppColors {
  // Ortak renkler
  static const Color primary = Color(0xFF2196F3); // Canlı mavi (gökyüzü tonu)
  static const Color accent = Color(0xFFFFC107); // Güneş sarısı

  // Açık tema renkleri
  static const Color lightBackground = Color(0xFFE3F2FD);
  static const Color lightCard = Color(0xFFFFFFFF);
  static const Color lightAppBar = Color(0xFF2196F3);
  static const Color lightText = Color(0xFF212121);
  static const Color lightTextFieldFill = Color(0xFFE0F7FA);
  static const Color lightTextFieldHint = Colors.grey;
  static const Color lightTextFieldBorder = primary;

  // Koyu tema renkleri
  static const Color darkBackground = Color(0xFF0D1B2A);
  static const Color darkCard = Color(0xFF1B263B);
  static const Color darkAppBar = Color(0xFF415A77);
  static const Color darkText = Color(0xFFE0E1DD);
  static const Color darkTextFieldFill = Color(0xFF1B263B);
  static const Color darkTextFieldHint = Colors.white38;
  static const Color darkTextFieldBorder = Color(0xFF64B5F6);
}
